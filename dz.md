## ДЗ: Генерация случайного 6-значного трек-номера и вставка в messages

### Цель задания

Научиться с помощью модуля `random` генерировать случайный 6-значный трек-номер и подставлять его в системный промпт перед отправкой сообщений модели.

---

### Задание

1. Импортируй модуль `random`.
2. Сгенерируй случайный **6-значный трек-номер**, состоящий только из цифр (например: `593471`).
3. Создай переменную `track_number` и запиши в неё сгенерированное число.
4. Добавь новый блок в список `messages` с ролью `"system"` и текстом:  
   `Трек-номер для нового заказа: <твой_номер>`
5. Размести этот блок **после блока с текущей датой**, но **до остального контекста** (orders, FAQ, chunks, history).
6. Убедись, что при каждом запуске программы генерируется новый номер.

---

### 📌 Подсказки

- Для генерации числа используй:  
  `random.randint(100000, 999999)`
- Если всё сделано правильно, номер будет разным при каждом запуске.

---

### ✅ Ожидаемый результат

В `messages` должен появиться блок вида:

```json
{"role": "system", "content": "Трек-номер для нового заказа: 582193"}
```

### Протестируйте диалог

Если трек-номер не добавится автоматически в новый заказ, то в промпте допишите соответствующее правило, например
```
- При добавлении нового заказа обязательно используй трек-номер
```

### Результат выполнения ДЗ

Сдайте на проверку ссылку на архив проекта или ссылку на github.
